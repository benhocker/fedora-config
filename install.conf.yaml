---
- clean:
  - "~"

- link:
  ~/.dotfiles: ""
  ~/.gitconfig: gitconfig
  ~/.ssh: ssh
  ~/.vim: vim/
  ~/.vimrc: vimrc
  ~/.zshrc:
    force: true
    path: zshrc

- shell:
  -
    - "git submodule update --init --recursive --remote"
    - "Install/updating submodules"
